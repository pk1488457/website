import {api} from "../core/api.js";import {escapeHtml,toast,formatDate} from "../core/ui.js";import {renderLayout} from "../components/layout.js";await renderLayout();
const grid=document.getElementById("resumeGrid");async function load(){try{const r=await api.get("/resumes");const rows=r.data||[];grid.innerHTML=rows.length?rows.map(x=>`<article class="resume-card"><div class="resume-thumb"><h4>${escapeHtml(x.title)}</h4><small>${escapeHtml(x.personalDetails?.headline||"Professional Resume")}</small><div class="line"></div><div class="line"></div><div class="line" style="width:75%"></div><div class="line" style="margin-top:30px"></div><div class="line"></div></div><div class="resume-card-body"><b>${escapeHtml(x.title)}</b><small>Updated ${formatDate(x.updatedAt)}</small><div class="resume-card-actions"><a class="btn btn-primary" href="/resume-builder.html?id=${x._id}">Edit</a><button class="btn-ghost duplicate" data-id="${x._id}">Duplicate</button><button class="icon-btn delete" data-id="${x._id}"><i class="fa-solid fa-trash"></i></button></div></div></article>`).join(""):`<div class="empty-state" style="grid-column:1/-1"><h3>Your first resume starts here</h3><p>Create a tailored, ATS-friendly resume in minutes.</p><button id="emptyCreate" class="btn btn-primary">Create resume</button></div>`;document.querySelectorAll(".duplicate").forEach(b=>b.onclick=async()=>{try{await api.post(`/resumes/${b.dataset.id}/duplicate`);toast("Resume duplicated");load()}catch(e){toast(e.message,"error")}});document.querySelectorAll(".delete").forEach(b=>b.onclick=async()=>{if(confirm("Delete this resume?"))try{await api.delete(`/resumes/${b.dataset.id}`);toast("Resume deleted");load()}catch(e){toast(e.message,"error")}});document.getElementById("emptyCreate")?.addEventListener("click",create)}catch(e){grid.innerHTML=`<div class="empty-state">${escapeHtml(e.message)}. Please sign in first.</div>`}}
async function create(){try{const r=await api.post("/resumes",{title:"Untitled Resume",personalDetails:{}});location.href=`/resume-builder.html?id=${r.data._id}`}catch(e){toast(e.message,"error")}}document.getElementById("createResume").onclick=create;load();
