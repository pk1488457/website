import {api} from "../core/api.js";import {toast} from "../core/ui.js";
const login=document.getElementById("loginForm"),reg=document.getElementById("registerForm"),forgot=document.getElementById("forgotForm"),reset=document.getElementById("resetForm");
async function submit(form,path,success){form?.addEventListener("submit",async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(form));try{await api.post(path,body);toast(success);setTimeout(()=>location.href=path.includes("forgot")?"/login.html":"/dashboard.html",700)}catch(err){toast(err.message,"error")}})}
submit(login,"/auth/login","Signed in successfully");submit(reg,"/auth/register","Account created successfully");submit(forgot,"/auth/forgotpassword","If the email exists, reset instructions have been sent");
reset?.addEventListener("submit",async e=>{e.preventDefault();const p=new URLSearchParams(location.search),token=p.get("token")||location.pathname.split("/").pop();const b=Object.fromEntries(new FormData(reset));if(b.password!==b.confirmPassword)return toast("Passwords do not match","error");try{await api.put(`/auth/resetpassword/${token}`,{password:b.password});toast("Password reset successfully");setTimeout(()=>location.href="/login.html",700)}catch(err){toast(err.message,"error")}});
document.querySelectorAll("[data-toggle-password]").forEach(b=>b.onclick=()=>{const i=document.querySelector(b.dataset.togglePassword);i.type=i.type==="password"?"text":"password"});
